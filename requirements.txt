# regime-trader — runtime dependencies
# Pinned to the versions in the test-green venv (py3.14, 102 tests passing).

# --- Regime detection / ML ---
hmmlearn==0.3.3
scikit-learn==1.8.0

# --- Broker / market data ---
alpaca-py==0.43.4          # the SDK actually used (alpaca-trade-api was legacy/dead)
yfinance==1.4.1            # keyless historical data source for backtests
simfin==1.0.2              # bulk fundamentals (ML v2): whole-universe download, no per-ticker 429

# --- Data / numerics ---
pandas==3.0.3
numpy==2.4.6
scipy==1.17.1
ta==0.11.0                 # technical analysis indicators

# --- Config / env ---
PyYAML==6.0.3
python-dotenv==1.2.2

# --- Dashboards ---
streamlit==1.58.0          # web dashboard: streamlit run monitoring/streamlit_app.py
plotly==6.7.0              # regime-confidence gauge in the dashboard
altair==6.1.0              # charts
rich==15.0.0               # legacy terminal dashboard / headless renderer

# --- Testing (dev) ---
pytest==9.0.3
