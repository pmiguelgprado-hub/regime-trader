# Default Streamlit dark theme — matches the reference dashboard
# (dark background, red accent, Source Sans font). Do not force monospace or a
# custom palette; the video uses stock dark.
[theme]
base = "dark"

[server]
headless = true
